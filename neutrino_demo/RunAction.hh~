#ifndef RUNACTION_H
#define RUNACTION_H

#include "G4UserRunAction.hh"
#include "G4Run.hh"
#include "TFile.h"
#include "TTree.h"

class RunAction : public G4UserRunAction {
public:
    RunAction();
    virtual ~RunAction();

    virtual void BeginOfRunAction(const G4Run*) override;
    virtual void EndOfRunAction(const G4Run*) override;

    static TTree* GetTree() { return fTree; }
    static TFile* GetFile() { return fFile; }

    // variables
    double E, x, y, z;

private:
    static TFile* fFile;
    static TTree* fTree;
};

#endif
